# Nightly Audit Findings — 2026-07-11

> **Note:** GitHub repo `Seckio89/connectradie` is private — the GitHub MCP token lacks private-repo access. Code-level scanning was performed on the local project files at `C:\Users\User\Desktop\project`. No auto-fix PR could be created via GitHub; findings below are the complete output.

---

## ✅ Code Quality — CLEAN

| Check | Result |
|-------|--------|
| `console.log` in `src/` | **0 occurrences** |
| TypeScript errors (`tsc --noEmit`) | **0 errors** |
| `any` type usage | **0** (single occurrence in a comment, not actual type) |
| Hardcoded API keys in source files | **None** |
| `.env` committed to git | **No — correctly gitignored** |
| Stripe webhook signature validation | **✅ `constructEventAsync` used correctly** |

---

## 🟡 Code — Low Risk (No Auto-Fix Available Without GitHub Access)

### `innerHTML` in Print/PDF Components

These are used exclusively for print/PDF generation, reading from React-rendered DOM — not from raw user input. XSS risk is low but worth noting.

| File | Line | Context |
|------|------|---------|
| `src/components/InvoiceViewModal.tsx` | 172 | `container.innerHTML = \`...\`` — wraps `printRef.current.innerHTML` for print |
| `src/pages/Payouts.tsx` | 595 | `container.innerHTML = finalHtml` — PDF via html2pdf.js |
| `src/components/LicenseCertificate.tsx` | 310 | `${printContent.innerHTML}` — print wrapper |

**Assessment:** All three inject content from React-controlled DOM refs. No user-supplied strings are interpolated without context. Low risk; no action required unless a future change introduces user data interpolation.

---

## 🔴 Supabase — Security (HIGH RISK — Log Only, Same as Jul 10)

### ERROR: Security Definer View

`public.public_vacancies` is defined with `SECURITY DEFINER` — bypasses the querying user's RLS.

**Fix:** Recreate without `SECURITY DEFINER` or add `SECURITY INVOKER`.
[Docs](https://supabase.com/docs/guides/database/database-linter?lint=0010_security_definer_view)

---

### WARN: Mutable Search Path on Function

`public.haversine_km` — vulnerable to search_path hijacking.

**Fix:** Add `SET search_path = public` to the function definition.
[Docs](https://supabase.com/docs/guides/database/database-linter?lint=0011_function_search_path_mutable)

---

### WARN: SECURITY DEFINER Functions Callable by `anon` (Unauthenticated)

These run with elevated privileges but are publicly callable without a session token:

| Function | Concern |
|----------|---------|
| `get_daily_profile_view_count(viewer_uuid uuid)` | Profile view data exposed to anon |
| `get_service_worker_details(p_recurring_job_id uuid)` | Worker details exposed to anon |
| `get_team_site_activity(p_since timestamp)` | Team activity exposed to anon — **potentially sensitive** |
| `has_user_engagement(user_uuid uuid)` | User engagement data exposed to anon |
| `notify_matching_tradies_new_vacancy()` | Trigger function callable by anon |
| `notify_service_assignment()` | Trigger function callable by anon |
| `search_businesses_by_name(search_term text)` | Likely intentional for public search — verify |

**Fix per function:** `REVOKE EXECUTE ON FUNCTION <fn> FROM anon;` or switch to `SECURITY INVOKER`.
[Docs](https://supabase.com/docs/guides/database/database-linter?lint=0028_anon_security_definer_function_executable)

---

### WARN: SECURITY DEFINER Functions Callable by `authenticated`

Review and revoke if the elevated privileges aren't needed:

| Function | Notes |
|----------|-------|
| `can_read_job_attachment(path text)` | Storage access check — verify INVOKER is insufficient |
| `create_notification(...)` | Should be server-side only; consider revoking from `authenticated` |
| `delete_user_account()` | Sensitive — likely intentional |
| `employer_approve_member` / `_decline_member` / `_remove_member` | Business logic — likely OK as DEFINER |
| `get_user_conversation_ids(uid uuid)` | Could be INVOKER |
| `is_admin()` | Common to keep as DEFINER |
| `is_conversation_creator(conv_id, user_id)` | Could be INVOKER |
| `is_tradie_verified(p_user_id uuid)` | Review |

[Docs](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable)

---

## 🆕 NEW Today — Supabase Performance

These were NOT present in yesterday's findings. All relate to the new geofencing (`site_visit_events`) table and recent team feature additions.

### Missing FK Indexes (NEW since Jul 10)

| Table | FK | Impact |
|-------|----|--------|
| `public.site_visit_events` | `site_visit_events_quote_id_fkey` | **NEW** — joins on `quote_id` without index |
| `public.recurring_jobs` | `recurring_jobs_assigned_team_member_id_fkey` | **NEW** — worker assignment queries slow |
| `public.client_contacts` | `client_contacts_linked_profile_id_fkey` | **NEW** — client detail page joins |

**Recommended fix (create in a new migration):**
```sql
-- Add missing FK indexes
CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_site_visit_events_quote_id
  ON public.site_visit_events (quote_id);

CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_recurring_jobs_assigned_team_member_id
  ON public.recurring_jobs (assigned_team_member_id);

CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_client_contacts_linked_profile_id
  ON public.client_contacts (linked_profile_id);
```

### Multiple Permissive Policies — NEW: `site_visit_events`

`site_visit_events` has two SELECT policies for `authenticated`:
- `Clients read site visit events for their jobs`
- `Tradies read own site visit events`

Both are evaluated for every query — merge them into a single `USING` clause with an `OR` condition for better performance.

---

## 🆕 NEW Today — New Code Review

### `stripe-payout-settings` Edge Function (Added Jul 10)

✅ **Well-implemented.** Observations:
- JWT auth validated server-side via `authClient.auth.getUser(token)` before any Stripe call
- Rate limiting applied (`10 req / 60s` per user) via `checkRateLimit`
- Role check enforces `tradie` only
- Correctly **blocks automatic payout schedules** — only `"manual"` is accepted, preserving the escrow model (critical for AFSL compliance)
- Falls back from `account_update` link to `createLoginLink` for Express accounts
- CORS origin locked to `connectradie.com` (reads from `ALLOWED_ORIGIN` env var)

No issues found.

### Geofencing Feature (Added Jul 10–11)

New files: `siteGeofence.ts`, `useSiteGeofencing.ts`, `SiteCheckInSetting.tsx`, `SiteGeofenceConsent.tsx`, `SiteLocationDisclosureModal.tsx`

✅ **Correct Play Store compliance approach** — consent recorded before any permission request.

⚠️ **One observation:** `siteGeofence.ts` uses `localStorage` for consent state (`connectradie_geofence_consent`). This is fine for web/Android but consent state won't persist across app uninstalls on Android. Consider migrating to Capacitor Preferences plugin for native persistence if this causes UX issues post-install.

---

## 🟡 Performance — Ongoing (Previously Logged)

**Unused indexes** (37 total, same as Jul 10 report — no new ones):
Low priority. Consider dropping after confirming they're not used by scheduled functions or Supabase edge functions. Key candidates: `idx_jobs_deleted_at`, `idx_jobs_coords`, `idx_profiles_base_coords`, `idx_profiles_employer_id`.

**Multiple permissive policies** (pre-existing):
- `business_team_members` INSERT
- `invoices` SELECT + UPDATE
- `jobs` SELECT/INSERT/UPDATE/DELETE
- `recurring_jobs` SELECT
- `recurring_sessions` SELECT

---

## Summary

| Category | Count | Status |
|----------|-------|--------|
| Security errors (Supabase) | 1 | 🔴 Ongoing — no change |
| Security warnings (Supabase) | 24 | 🟡 Ongoing — no change |
| New FK indexes needed | 3 | 🆕 New today |
| Code quality issues | 0 | ✅ Clean |
| TypeScript errors | 0 | ✅ Clean |
| Secrets exposed | 0 | ✅ Clean |
| New edge function issues | 0 | ✅ Clean |

**Action recommended:** Create a migration adding the 3 missing FK indexes from the geofencing and team features.
