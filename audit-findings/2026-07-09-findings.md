# Nightly Audit — 2026-07-09

## Run status

| Step | Result |
|------|--------|
| GitHub code audit (search, auto-fix PR) | **Blocked** — repo not reachable |
| Supabase advisors (security + performance) | **Completed** |
| Auto-fix PR | Not created (GitHub blocked) |

### GitHub access failure
The task targets `Seckio89/connectradie` (master). Every GitHub API call returned **404 Not Found**:

- `get_file_contents` on `package.json` → 404
- `list_branches` → 404
- Tried variants `ConnecTradie`, `connec-tradie`, `connectradie-app` → all 404
- `search_repositories user:Seckio89` → 422 (account has 0 public repos, token cannot see private repos)
- `get_me` succeeded (login `Seckio89`, 0 public repos) — so the token is valid but **lacks access to the repo** (private + missing `repo` scope, or the repo does not exist under this name/owner).

**Action needed from you:** confirm the exact repo owner/name and grant the GitHub connector `repo` scope for private repositories. Until then the code-search / console.log / `any` / secret-scan steps and the auto-fix PR cannot run.

The code-quality auto-fix portion produced **0 fixes** this run (could not read source).

---

## Supabase advisors — project `Connectradie` (`uoqygmizupdpanplpvor`)

All database findings are **log-only** per audit policy (RLS / security / auth / payment code is never auto-modified). Review and remediate manually.

### Security — 1 ERROR

**Security Definer View** — `public.public_vacancies`
The view runs with the creator's permissions, bypassing the querying user's RLS. Confirm this is intentional; if not, recreate it as `security_invoker`.
Remediation: https://supabase.com/docs/guides/database/database-linter?lint=0010_security_definer_view

### Security — WARN

**Function search_path mutable** — `public.haversine_km`
Set an explicit `search_path` to prevent search-path injection.
https://supabase.com/docs/guides/database/database-linter?lint=0011_function_search_path_mutable

**SECURITY DEFINER functions executable by `anon` (7)** — callable unauthenticated via `/rest/v1/rpc/…`. Revoke `EXECUTE` from `anon` or switch to `SECURITY INVOKER` unless deliberately public:
`get_daily_profile_view_count`, `get_service_worker_details`, `get_team_site_activity`, `has_user_engagement`, `notify_matching_tradies_new_vacancy`, `notify_service_assignment`, `search_businesses_by_name`
https://supabase.com/docs/guides/database/database-linter?lint=0028_anon_security_definer_function_executable

**SECURITY DEFINER functions executable by `authenticated` (17)** — review whether signed-in users should be able to call these directly:
`can_read_job_attachment`, `create_notification`, `delete_user_account`, `employer_approve_member`, `employer_decline_member`, `employer_remove_member`, `get_daily_profile_view_count`, `get_service_worker_details`, `get_team_site_activity`, `get_user_conversation_ids`, `has_user_engagement`, `is_admin`, `is_conversation_creator`, `is_tradie_verified`, `notify_matching_tradies_new_vacancy`, `notify_service_assignment`, `search_businesses_by_name`
https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable

> Note: several of these (`is_admin`, `is_conversation_creator`, `is_tradie_verified`, notify triggers) are legitimately SECURITY DEFINER helpers used inside RLS/triggers. Flagged for review, not necessarily a defect.

### Performance — WARN / INFO

**Unindexed foreign keys (3)** — add covering indexes:
`client_contacts.client_contacts_linked_profile_id_fkey`, `recurring_jobs.recurring_jobs_assigned_team_member_id_fkey`, `site_visit_events.site_visit_events_quote_id_fkey`
https://supabase.com/docs/guides/database/database-linter?lint=0001_unindexed_foreign_keys

**Multiple permissive policies (8)** — same role+action evaluated per query; consider consolidating:
`business_team_members` (INSERT), `jobs` (DELETE/INSERT/SELECT/UPDATE), `recurring_jobs` (SELECT), `recurring_sessions` (SELECT), `site_visit_events` (SELECT)
https://supabase.com/docs/guides/database/database-linter?lint=0006_multiple_permissive_policies

**Unused indexes (≈40)** — never used; candidates for removal once traffic is representative (young project — usage stats may simply not have accumulated yet). Includes indexes on `jobs`, `profiles`, `invoices`, `time_entries`, `service_*`, `recurring_invoices`, `disputes`, `platform_*`, etc.
https://supabase.com/docs/guides/database/database-linter?lint=0005_unused_index

**Auth connection strategy (INFO)** — Auth is capped at 10 absolute connections; switch to percentage-based before scaling instance size.
https://supabase.com/docs/guides/deployment/going-into-prod

---

## Summary counts

- Security: **1 error**, **~25 warnings** (1 search_path + 7 anon-exec + 17 authenticated-exec)
- Performance: **3 unindexed FKs**, **8 multiple-permissive-policy**, **~40 unused indexes**, **1 auth-connection info**
- Auto-fixes applied: **0** (GitHub repo unreachable)
- PR created: **no**

_Generated automatically. No database or code changes were made._
