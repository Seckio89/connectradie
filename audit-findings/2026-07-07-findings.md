# Nightly Audit — 2026-07-07

## Run status: PARTIAL

The GitHub portion of the audit could not run. The Supabase database audit completed.

---

## BLOCKER — GitHub repo not reachable

The task targets GitHub repo `Seckio89/connectradie` (master), but the GitHub API returns **404 Not Found** for that repo and all name variations tried (`connectradie`, `ConnecTradie`, `connectradie-app`). The authenticated account (`Seckio89`, William) shows **0 public repos** and repo search returns no accessible repositories.

Because no repo could be read, these steps were skipped:
- Code-pattern searches (secrets, `console.log`, `any`, `dangerouslySetInnerHTML`/`eval`)
- Auto-fix branch, commits, and PR

**Likely causes / to check:**
- Repo name or owner differs from `Seckio89/connectradie` (check exact slug on GitHub).
- Repo is private and the connected GitHub token lacks `repo` scope / access.
- Repo hasn't been pushed to GitHub yet (code exists locally only).

**Action needed from you:** confirm the exact repo slug and that the GitHub connector has access. Once fixed, the code audit + auto-fix PR flow will run normally.

---

## Supabase advisors — Connectradie project (`uoqygmizupdpanplpvor`)

All items below are database/security concerns — **log-only, not auto-fixed** (RLS/security tier per audit policy). Fix via a reviewed migration.

### Security

**ERROR (1)**
- **Security Definer View** — `public.public_vacancies` is defined `SECURITY DEFINER`, so it enforces the creator's RLS rather than the querying user's. Review whether this is intentional. Remediation: https://supabase.com/docs/guides/database/database-linter?lint=0010_security_definer_view

**WARN — Function search_path mutable (1)**
- `public.haversine_km` has a role-mutable `search_path`. Set `search_path` explicitly. Remediation: https://supabase.com/docs/guides/database/database-linter?lint=0011_function_search_path_mutable

**WARN — SECURITY DEFINER functions executable by `anon` (5)**
Callable without signing in via `/rest/v1/rpc/*`. Revoke EXECUTE, switch to SECURITY INVOKER, or move out of the exposed schema if not intended to be public:
- `get_daily_profile_view_count(viewer_uuid uuid)`
- `has_user_engagement(user_uuid uuid)`
- `notify_matching_tradies_new_vacancy()`
- `notify_service_assignment()`
- `search_businesses_by_name(search_term text)`

Remediation: https://supabase.com/docs/guides/database/database-linter?lint=0028_anon_security_definer_function_executable

**WARN — SECURITY DEFINER functions executable by `authenticated` (14)**
Review each; revoke EXECUTE / switch to SECURITY INVOKER if signed-in users shouldn't call them:
`can_read_job_attachment`, `create_notification`, `delete_user_account`, `employer_approve_member`, `employer_decline_member`, `employer_remove_member`, `get_daily_profile_view_count`, `get_user_conversation_ids`, `has_user_engagement`, `is_admin`, `is_conversation_creator`, `is_tradie_verified`, `notify_matching_tradies_new_vacancy`, `notify_service_assignment`, `search_businesses_by_name`.

Remediation: https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable

_Note: several of these (`is_admin`, `is_tradie_verified`, `is_conversation_creator`, `create_notification`, etc.) are commonly intended as SECURITY DEFINER helpers used inside RLS policies — verify before changing, as switching to INVOKER may break policies._

### Performance

- **Unindexed foreign keys (2):** `recurring_jobs.assigned_team_member_id`, `site_visit_events.quote_id`. Add covering indexes.
- **Unused indexes (~40):** many indexes never used (e.g. on `service_description_raw`, `platform_recommendations`, `time_entries`, `invoices`, `jobs`, `profiles`, `disputes`, `recurring_invoices`). Candidates for removal — but confirm the app/data is mature enough that "unused" is meaningful before dropping.
- **Multiple permissive policies (4):** `business_team_members` (INSERT), `recurring_jobs` (SELECT), `recurring_sessions` (SELECT), `site_visit_events` (SELECT). Consolidate policies for the same role+action.
- **Auth DB connections (INFO):** Auth server capped at 10 absolute connections; switch to percentage-based allocation before scaling instance size.

---

## Summary
- **GitHub audit:** blocked (repo not found) — 0 auto-fixes, 0 code findings.
- **Supabase:** 1 error, 20 security warnings, ~46 performance items — all log-only.
- **Auto-fixes applied:** 0 (no reachable repo; all DB findings are high-risk by policy).
