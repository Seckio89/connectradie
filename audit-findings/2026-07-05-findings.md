# Nightly Audit — 2026-07-05

## Run status: PARTIAL — GitHub code audit could not run

The GitHub MCP authenticated as `Seckio89`, but `Seckio89/connectradie` returned **404 Not Found** on every API call (file reads, commits, repo search). The account also reports 0 accessible repos via search. Most likely cause: the GitHub connector token doesn't have this private repo granted (e.g. fine-grained PAT / app installation without repo access), or the repo was renamed/moved.

**Consequence:** no code searches, no auto-fix branch, no PR this run. This file is written to the local project folder instead of the `auto-fixes/2026-07-05` branch because no GitHub write access was possible.

**Action needed:** re-grant the GitHub connector access to `Seckio89/connectradie`, then the nightly audit will resume normally.

## Supabase advisors (project: Connectradie, `uoqygmizupdpanplpvor`)

### Security — 1 ERROR, 21 WARN

**ERROR — Security Definer View** (high-risk, log only — not auto-fixed):
- View `public.public_vacancies` is defined with SECURITY DEFINER, so it bypasses the querying user's RLS. [Remediation](https://supabase.com/docs/guides/database/database-linter?lint=0010_security_definer_view)

**WARN — SECURITY DEFINER functions callable by `anon` (5)** — callable without signing in via `/rest/v1/rpc/...`:
`get_daily_profile_view_count`, `has_user_engagement`, `notify_matching_tradies_new_vacancy`, `notify_service_assignment`, `search_businesses_by_name`. Revoke EXECUTE from `anon` or switch to SECURITY INVOKER if unintentional. [Remediation](https://supabase.com/docs/guides/database/database-linter?lint=0028_anon_security_definer_function_executable)

**WARN — SECURITY DEFINER functions callable by `authenticated` (16)**, notably sensitive ones:
`delete_user_account`, `create_notification`, `is_admin`, `employer_approve_member`, `employer_decline_member`, `employer_remove_member`, `can_read_job_attachment`, `is_tradie_verified`, `is_conversation_creator`, `get_user_conversation_ids`, plus the five listed above. Review whether each is intended to be a public RPC — `create_notification` in particular may allow any signed-in user to create notifications for arbitrary users. [Remediation](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable)

All security items touch RLS/auth surface → **high-risk, logged only per audit policy. No changes applied.**

### Performance — 1 WARN, ~45 INFO

- WARN: `public.business_team_members` has multiple permissive INSERT policies for `authenticated` ("Business owner can insert team members" + "Members can request to join a team") — every insert evaluates both. Consider consolidating. [Remediation](https://supabase.com/docs/guides/database/database-linter?lint=0006_multiple_permissive_policies)
- INFO: unindexed foreign key `recurring_jobs.assigned_team_member_id` (`recurring_jobs_assigned_team_member_id_fkey`). A covering index would fit the migration-scaffolder convention.
- INFO: ~43 unused indexes (candidates for removal after confirming they're not for new features), across `recurring_invoices`, `service_description_raw/keywords`, `saved_payment_methods`, `platform_recommendations`, `lead_impressions`, `conversations`, `service_agreements`, `jobs`, `reviews`, `admin_audit_log`, `invoices`, `invoice_line_items`, `app_settings`, `milestone_subcontractors`, `profiles`, `time_entries`, `platform_updates`, `user_update_reads`, `service_invoices`, `service_visits`, `disputes`, `payments`, `typing_indicators`.
- INFO: Auth server uses a fixed 10-connection cap; switch to percentage-based allocation before scaling instance size. [Guide](https://supabase.com/docs/guides/deployment/going-into-prod)

## Summary

| Category | Count | Action |
|---|---|---|
| GitHub code audit | — | Blocked (repo 404) — fix connector access |
| Supabase security ERROR | 1 | Logged (high-risk) |
| Supabase security WARN | 21 | Logged (high-risk) |
| Supabase performance WARN | 1 | Logged |
| Supabase performance INFO | ~45 | Logged |
| Auto-fixes applied | 0 | No repo access |
| PR created | No | No repo access |
