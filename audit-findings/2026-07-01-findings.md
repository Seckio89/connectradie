# Nightly Audit — ConnecTradie — 2026-07-01

**Run mode:** automated (unattended). **Repo:** Seckio89/connectradie. **Working copy:** `~/Desktop/project`.

## ⚠️ Run outcome: audit completed, but the auto-fix/PR workflow was BLOCKED and no code was changed.

Three conditions made it unsafe and impossible to perform the git write steps (branch, commit, push, PR) that the task normally does. **No files were modified, staged, committed, deleted, pushed, or PR'd.** This is a report-only run.

### Blocker 1 — Git repository is corrupted
`git diff` **segfaults** (`Segmentation fault (core dumped)`), and `git fsck` reports a **missing blob** and a **broken ref**:

```
error: refs/test123: invalid sha1 pointer 0000000000000000000000000000000000000000
missing blob 6a51eea55abe17a1d3c00805217b62c053c9c9e7
```

Running branch/checkout/commit operations on a repo with unreadable objects risks worsening the corruption or losing work. Recommend repairing locally before any automated git activity resumes (see remediation below).

### Blocker 2 — No git credentials in this environment
`git ls-remote origin` fails with `could not read Username for 'https://github.com'`, and there is no `gh` CLI. **`git pull`, `git push`, and PR creation are therefore impossible here** regardless of repo health.

### Blocker 3 — Uncommitted work in the working tree
The tree has staged **and** unstaged changes (a deleted `OneflareBanner.tsx`, plus modifications to `index.html`, `HeroSection.tsx`, `UserTradeBadges.tsx`, `ClientDashboard.tsx`, `Invoice.tsx`, `LandingPage.tsx`, `mobile-responsive.css`). The local branch is 2 commits ahead of `origin/master`. Auto-fixing files now would mix audit edits into your in-progress work with no clean way to isolate them into a separate branch/commit (compounded by Blocker 1).

### Also note — branch mismatch
The task assumes `main`; this repo's default branch is **`master`**. There is no `main` branch.

---

## Audit results (read-only static analysis)

The good news: the application source is in solid shape. Prior audit cycles (there are existing `AUDIT-REPORT-*.md` files and a recent `feat(seo): implement 3 weekly audit recommendations` commit) appear to have cleared the usual low-hanging fruit.

**Clean:**
- **No stray `console.log`/`console.debug`** in production `src/` — all 130+ console calls are legitimate `console.error`/`console.warn`/`console.info` in catch blocks.
- **No hardcoded secrets** — no Stripe/Google/Supabase keys or JWTs in `src/`. `.env` is correctly git-ignored and untracked.
- **No `dangerouslySetInnerHTML`** (no obvious XSS surface).
- **No `TODO`/`FIXME`/`HACK`** markers; only one `: any` match, and it's inside a code comment (not a real type).
- **RLS coverage looks complete** at table-creation level: 47 migrations with `CREATE TABLE`, 47 with `ENABLE ROW LEVEL SECURITY`. 235 `CREATE INDEX` statements — no obvious systemic FK-index gaps in a spot check (authoritative check recommended via Supabase advisors, below).

### Low-risk items (would normally auto-fix — logged only this run)

ESLint reports **181 problems (129 errors, 52 warnings)**. The safely-fixable subset, in **non-payment / non-auth** files:

| Rule | Count | Auto-fixable | Notes |
|---|---|---|---|
| `prefer-const` | 5 | yes (`--fix`) | Simple `let`→`const`. One is in a build artifact (see below). |
| `no-useless-escape` | 49 | yes (`--fix`) | Redundant `\/` etc. in regexes/strings. **~6 are in payment functions** (`parse-invoice`, `auto-release-payments`, `generate-auto-invoices`, `calculate-job-fees`) — exclude those. |
| `no-unused-vars` | 71 | manual | Many are in payment edge functions (excluded). |

Roughly **39 problems sit in plain `src/components` and `src/pages`** (excluding payment/auth/invoice files) and would be the candidate auto-fix set in a healthy repo.

**Tooling nit (low-risk):** ESLint is linting a build artifact — `android/app/build/intermediates/assets/debug/mergeDebugAssets/native-bridge.js`. Add `android/**/build/**` to the ESLint ignore list so generated files don't pollute results.

### Do-not-auto-fix items (logged for manual review)

| # | Severity | Area | Location | Description | Recommended fix |
|---|---|---|---|---|---|
| 1 | **High** | TypeScript / Billing | `src/pages/TradieDashboard.tsx` (~L520–545, L1130, L1169) | `npm run typecheck` **fails**. Queries reference tables `recurring_invoices` and `lead_impressions` that are absent from the generated Supabase types, and `.profiles` is accessed on a `Job` type that lacks it. Errors: `TS2769` (no overload matches), `TS2339` (property does not exist). | Regenerate Supabase types (`supabase gen types` / the MCP `generate_typescript_types`) so types match the live schema, then re-typecheck. Touches billing/schema types — human review required. Do **not** auto-apply. |
| 2 | **Medium** | React correctness | 46 sites across components/pages | `react-hooks/exhaustive-deps` warnings — missing/incorrect `useEffect`/`useCallback` dependency arrays. | Review case-by-case. Auto-"fixing" dependency arrays can introduce infinite loops or change fetch behavior — **not** safe to bulk-fix. |
| 3 | **Medium** | Payment/billing code quality | `pay-price-increase`, `release-escrow`, `stripe-connect-onboarding`, `auto-release-payments`, `generate-auto-invoices`, `parse-invoice`, `AdminPayments.tsx`, `PaymentHistory.tsx`, `Payouts.tsx`, `CreateInvoiceModal.tsx`, `InvoiceViewModal.tsx` | Unused imports/vars and redundant escapes inside payment/billing/invoice code. | Per task policy, payment/billing code is **never** auto-fixed. Clean up manually with test coverage. |
| 4 | **Medium** | Auth code quality | `src/contexts/AuthContext.tsx` | `prefer-const` / `no-useless-escape` inside auth logic. | Per policy, auth code is not auto-fixed. Fix manually. |
| 5 | **Info** | DB verification | Supabase migrations | Static coverage looks good, but FK-index and RLS completeness can't be fully proven from files alone. | Run Supabase advisors (`get_advisors` for `security` and `performance`) against the live project for an authoritative RLS/missing-index/exposed-data report. |

---

## Recommended remediation (for the user to run locally, where git auth exists)

1. **Repair the repo.** Back up first, then:
   ```
   git update-ref -d refs/test123          # remove the broken ref
   git fetch origin && git fsck --full     # confirm the missing blob is recoverable from origin
   # if the missing blob persists, re-clone into a fresh directory and re-apply the WIP
   ```
2. **Deal with the uncommitted WIP** — commit or stash it so audit runs start from a clean tree.
3. **Fix the branch assumption** — the scheduled task targets `main`, but the repo uses `master`. Update the task (or add a `main` branch).
4. **Provide git credentials to the automation environment** (a token / `gh auth`) so the push + PR steps can actually run.
5. Once healthy, the ~39 non-sensitive lint items are safe to auto-fix (`eslint . --fix` scoped to non-payment/non-auth files), and the TradieDashboard type errors should be addressed by regenerating Supabase types.

*Note: the environment could not reach the local clone as a normal filesystem until a folder was connected; the task's `~/Desktop/project` path was mounted read/write for this audit.*
