# Nightly Audit — 2026-07-08

**Repo target:** Seckio89/connectradie (master)
**Supabase project:** Connectradie (`uoqygmizupdpanplpvor`, ap-south-1, ACTIVE_HEALTHY)

---

## ⚠️ Blocker: GitHub repo not accessible

The GitHub-based portion of the audit could **not** run. The GitHub MCP token authenticates as `Seckio89` (William, Connectradie), but:

- `GET /repos/Seckio89/connectradie` → **404 Not Found** (also tried `ConnecTradie` casing)
- `search_repositories user:Seckio89` → 422 (no visible repos)
- `get_me` reports **public_repos: 0**

Likely causes: the repo hasn't been pushed to GitHub yet, was renamed, or is private under a different owner/org and the connected token lacks access to it.

**Consequence:** no code search, no auto-fix branch, and no PR were created this run. Once the repo is pushed or the token is granted access, the GitHub steps (secret scan, `console.log`/`any`/`dangerouslySetInnerHTML` search, auto-fix PR) will run normally.

No writes were made anywhere (no branch, no PR, no DB changes) — this is a report-only run.

---

## Supabase advisors (ran successfully)

### Security — 1 ERROR, many WARN

**ERROR — Security Definer View**
- `public.public_vacancies` is defined `SECURITY DEFINER`, so it enforces the creator's RLS rather than the querying user's. Review whether this is intentional; if not, recreate the view without `SECURITY DEFINER`.
- Ref: https://supabase.com/docs/guides/database/database-linter?lint=0010_security_definer_view

**WARN — Function search_path mutable**
- `public.haversine_km` has a role-mutable `search_path`. Set `search_path` explicitly (e.g. `SET search_path = ''`).
- Ref: https://supabase.com/docs/guides/database/database-linter?lint=0011_function_search_path_mutable

**WARN — SECURITY DEFINER functions executable by `anon`** (0028)
Callable unauthenticated via `/rest/v1/rpc/...`. Review each; revoke `EXECUTE` from `anon` or switch to `SECURITY INVOKER` where public access isn't intended:
- `get_daily_profile_view_count(viewer_uuid uuid)`
- `get_team_site_activity(p_since timestamptz)`
- `has_user_engagement(user_uuid uuid)`
- `notify_matching_tradies_new_vacancy()` (trigger fn — likely fine, but should not be RPC-exposed)
- `notify_service_assignment()` (trigger fn — same)
- `search_businesses_by_name(search_term text)`
- Ref: https://supabase.com/docs/guides/database/database-linter?lint=0028_anon_security_definer_function_executable

**WARN — SECURITY DEFINER functions executable by `authenticated`** (0029)
Review that signed-in-user access is intended for each:
`can_read_job_attachment`, `create_notification`, `delete_user_account`, `employer_approve_member`, `employer_decline_member`, `employer_remove_member`, `get_daily_profile_view_count`, `get_team_site_activity`, `get_user_conversation_ids`, `has_user_engagement`, `is_admin`, `is_conversation_creator`, `is_tradie_verified`, `notify_matching_tradies_new_vacancy`, `notify_service_assignment`, `search_businesses_by_name`
- Ref: https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable

> **All of the above are high-risk (auth/RLS/security) and were NOT auto-fixed.** They touch access control and payment-adjacent code — log-and-review only, per audit policy.

### Performance — WARN + INFO

**WARN — Multiple permissive policies** (extra RLS evaluation per query):
- `business_team_members` (authenticated / INSERT): "Business owner can insert team members" + "Members can request to join a team"
- `recurring_jobs` (authenticated / SELECT): "Assigned worker can view service" + "Users can manage their own recurring jobs"
- `recurring_sessions` (authenticated / SELECT): "Assigned worker can view service sessions" + "Users can manage relevant sessions"
- `site_visit_events` (authenticated / SELECT): "Clients read site visit events for their jobs" + "Tradies read own site visit events"
- Consider consolidating each pair into a single policy with `OR` logic.

**INFO — Unindexed foreign keys** (safe to add covering indexes):
- `recurring_jobs.assigned_team_member_id` (`recurring_jobs_assigned_team_member_id_fkey`)
- `site_visit_events.quote_id` (`site_visit_events_quote_id_fkey`)

**INFO — Unused indexes (~42)** — candidates for removal after confirming they aren't needed for planned features. Notable ones: `idx_jobs_deleted_at`, `idx_jobs_coords`, `idx_profiles_base_coords`, `idx_profiles_employer_id`, `idx_saved_payment_methods_pm`, `idx_saved_payment_methods_tradie_id`, `idx_payments_parent_payment_id`, plus many on `recurring_invoices`, `service_*`, `milestone_subcontractors`, `time_entries`, `platform_*`, `disputes`, `invoices`, `invoice_line_items`, `admin_audit_log`, `typing_indicators`, `conversations`, `reviews`, `lead_impressions`, `app_settings`, `user_update_reads`, `service_description_*`.
  (Unused-index removal is low-risk but should be batched into a reviewed migration, not auto-applied.)

**INFO — Auth DB connection strategy** — Auth server capped at 10 absolute connections; switch to percentage-based allocation so it scales with instance size.
- Ref: https://supabase.com/docs/guides/deployment/going-into-prod

---

## Recommended next actions
1. Fix the GitHub connection (push the repo, or grant the token access) so code-level auto-fixes resume.
2. Address the SECURITY DEFINER **view** (ERROR) first.
3. Triage the anon-executable RPC functions — especially `search_businesses_by_name`, `has_user_engagement`, `get_*` — and revoke `anon` EXECUTE where not intended.
4. Add the two missing FK indexes via a new migration (never edit existing migrations).
